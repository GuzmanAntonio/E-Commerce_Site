import React from 'react'

const styles = {
  container: {
    display: 'flex',
    backgroundColor: '#FDE3A7',
    flexDirection: 'column',
    width: '100vw',
    height: 'auto',
    padding: '20px'
  }
}

const Home = () =>
  <div style={styles.container}>
    <h1>Home</h1>
  </div>

export default Home
